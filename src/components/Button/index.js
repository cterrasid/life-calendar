import React from 'react';
import './styles.scss';

const Button = () => {
  return (
    <label htmlFor="save" title="save">
      <input type="button" />
    </label>
  );
};

export default Button;
